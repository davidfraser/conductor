Conductor
----------

This is a visual metronome that tries to act like a conductor's baton.

pygame is the only dependency.

Use is simple - just run the script with one argument, the number of beats per minute. You can optionally add 2/3/4 beats per bar as the second argument.

